---
import type { NavSection } from "../../data/navigation";

interface Props {
    section: NavSection;
}

const { section } = Astro.props;
---

<div class="space-y-3 sm:space-y-4">
    <h3
        class="text-secondary pb-1.5 text-xl font-bold uppercase tracking-wide sm:pb-2 sm:text-2xl lg:text-3xl"
    >
        {section.title}
    </h3>

    <ul
        class="space-y-2 text-base font-light leading-relaxed text-gray-200 sm:space-y-2.5 sm:text-lg lg:text-xl xl:text-2xl"
    >
        {
            section.items.map((item) => (
                <li>
                    <a
                        href={item.href}
                        class="inline-block py-0.5 transition-colors duration-200 hover:text-white"
                    >
                        {item.label}
                    </a>
                </li>
            ))
        }
    </ul>
</div>
